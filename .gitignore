/mieru-web-ui
/web/dist/
# Built SPA — generated by `npm run build`, embedded at build time (CI builds it).
/internal/webfs/dist/*
!/internal/webfs/dist/.gitkeep
/web/node_modules/
/data/
*.db
.DS_Store
web/tsconfig.tsbuildinfo

# Local config and runtime state (compose volumes, dev data)
.env
docker-compose.override.yml
/panel-data/
/mita-config/
/.devdata/
